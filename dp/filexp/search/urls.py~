from django.conf.urls import patterns , url
from search import views
urlpatterns = patterns( '',
       url(r'^$' , views.index , name='index'),
       url(r'^misearch/$', views.misearch, name='misearch'),
       url(r'^(?P<pk>\d+)/exd/$', views.exd,name='detail'),
       
       
)

